server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Routage côté client (react-router) : toute route inconnue retombe sur index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
