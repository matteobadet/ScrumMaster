apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Manifests de base ScrumMaster — isolés de SkillForge (Constitution Principe V).
# Complétés en Phase 7 (Polish) : Deployment API + frontend, Service, ConfigMap.
resources: []
